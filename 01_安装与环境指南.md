# 安装与环境指南

## A. 推荐方式：交给 Codex 执行

打开整个工作台目录，把 `00_从这里开始.md` 的首次提示发给 Codex。这里的“安装”只把技能文件复制到本机技能目录，不安装 Python 科学计算库，不运行上游脚本，不联网、不索取 API Key。

默认安装目录为 `$CODEX_HOME/skills`，未设置时为 `~/.codex/skills`。安装后在下一轮对话调用 `$danta-proposal-guide`。如果当前客户端仍未发现，可新建会话；也可直接要求 AI 阅读包内的 SKILL.md 继续盘点。

## B. 手动安装（可选）

在终端进入解压后的工作台文件夹。macOS/Linux 使用 `python3`；Windows 通常使用 `py -3`，将下列命令的 `python3` 替换即可。安装器要求 Python 3.9 或更新版本，只使用标准库。

```bash
python3 scripts/install_skills.py --verify
python3 scripts/install_skills.py --dry-run
python3 scripts/install_skills.py
python3 scripts/check_environment.py
```

- `--verify`：只检查包内安装文件，不复制。
- `--dry-run`：显示目标路径和冲突，不写入。
- 默认执行：安装尚不存在的技能；相同版本跳过，不同版本保留现有目录并报告冲突。
- `--dest`：指定技能安装目录。例：`python3 scripts/install_skills.py --dest /你的路径/skills`。
- `--only danta-proposal-guide`：仅安装主引导技能。
- 环境检查使用相同自定义目录时：`python3 scripts/check_environment.py --skills-dir /你的路径/skills`。

不需要 sudo 或管理员权限。不提供覆盖开关。冲突时请先比较现有版本；如确需替换，手动备份旧目录再重跑。不要为解决冲突删除其他技能。

环境报告默认写到 `我的开题/02_盘点/环境检查_时间戳.md`。它只检查 Python、指定技能目录、项目读写能力及若干可选工具是否可找到，不读取密钥，不扫描其他研究资料，不验证联网检索或实验平台。

## C. 无 Python 或没有桌面工具怎么办？

先让 AI 阅读 `skills/danta-proposal-guide/SKILL.md` 及所需 references，仍可开始访谈与模板整理。能否执行脚本与持久保存文件取决于客户端；普通网页聊天上传文件不等同于安装 skills，AI 必须如实说明能力。需要脚本时再从 Python 官方网站 https://www.python.org/downloads/ 获取 Python 3.9+，安装后重试。

支持 Agent Skills 的其他客户端可以使用 `--dest` 指向该客户端当前文档规定的技能目录；本包未替每种客户端验证自动发现机制。

## D. 安装完成不等于能力就绪

| 能力 | 首次怎样验证 | 暂时不可用时 |
|---|---|---|
| 技能被发现 | 下一轮显式调用主引导 skill | 直接读包内 SKILL.md |
| 文献检索 | 实际执行一次研究方向相关的公开检索，保存检索式、日期和返回记录 | 整理用户文献，标注“尚未联网核查” |
| 引文核验 | 打开一篇原始研究，核对题名、作者、年份和 DOI/PMID | 标为待核实，不填造标识符 |
| PDF 读取 | 读取用户指定的一篇，报告题名、页数及是否需要 OCR | 请求可读版本或按需配置 OCR |
| 数据读取 | 对去标识的指定样例做字段和缺失检查 | 先整理数据字典 |
| 实验条件 | 根据用户、平台说明或已有记录确认 | 标明用户自述/待核实 |

Git、R、Pandoc、OCR 和科学计算库均不是第一轮盘点的前置条件。统计、作图、全文检索等扩展依赖只在任务确实需要时配置，并记录具体工具与版本。

## E. 常见问题

- **完整性检查失败**：保留错误信息，重新解压原始包。若文件经过有意修改，先核查差异；不要把删除校验当成修复。
- **安装冲突**：现有技能被保留。环境报告会区分与包一致/不同；请 AI 比较版本与依赖，不能宣称已更新。
- **GitHub 或 API 不可达**：本包离线安装不受影响；联网文献功能单独标记受限。
- **上游要求额外绘图/API 服务**：先读对应技能说明与 `03_Skills来源与边界.md`，仅安装本包不代表已经配置这些服务。
- **重新开始新会话**：发“先读我的开题/00_状态与下一步.md，再继续”。

## F. 从 v1.0 / v1.1 更新到 v1.2

本版保留七个上游科研技能的同一固定版本，只修改主引导和工作台资料。已安装旧版时，七个上游技能应显示相同并跳过，主引导会显示冲突。

先保留旧工作区和真实研究记录。可让 AI 比较两个版本的主引导，将旧安装目录 `danta-proposal-guide` 移到你选定的备份目录（放在技能发现目录之外），再运行本版安装器；这一步应在你明确要求更新时做，安装器本身不自动覆盖。

已有真实研究记录不要用新模板替换。让 AI 把新的思维引导说明及思路手记结构合并到现有工作区，保留原内容和决策。若暂不更新安装，也可明确让 AI 阅读本包 `skills/danta-proposal-guide/SKILL.md` 及其 references 按新版工作。

若旧路径有日报或其他自动化写入，新目录不会自动接管。先在应用中核对已有任务的目标路径，再按用户明确要求更新；本包不创建、复制或修改自动化。不要把旧模板或模拟测试场景迁入个人研究画像。
