# v1.7 科研知识库接入验证

日期：2026-09-24。测试输入均为空白模板或明确虚构文本，不包含真实研究资料。参考hwm的结构原则后重新编写；不修改hwm，不发布其原文件或个人内容。

## 交付范围

15个顶层编号中英文业务目录，每层00_index；19项旧记录到新库的唯一映射；12类笔记模板；核心Templates/附件/链接配置；可见女娲顾问位置；实验→数据集→分析运行→图表→成果的登记规则；来源、范围、日期和复核条件明确的记忆协议。

旧工作台仍可用；进入新vault后主引导先读标记与Path-Map，不再创建“我的开题”平行台账。只提供人工核验的迁移流程，不执行旧资料自动搬移。

## 实际验证

- 新目录初始化、dry-run不写入、主引导及角色复制。
- 重复初始化保留模拟研究记录和自定义Obsidian配置；拒绝已存在非标记目录、空路径和符号链接目标。
- 模板篡改/来源符号链接拒绝，失败不留下目标目录。
- 19项映射存在且不重复、索引和本地链接可解析、元数据字段与JSON可读；刻意断链与越界映射会被检测。
- 原发行集成检查继续覆盖12个技能目录的完整性、安装/重复/冲突、工作副本边界与TOML。

检查器只检查结构；不验证文献、临床或科学事实，不执行联网检索、真实实验、长期记忆行为评测或用户设备上的Obsidian UI。Markdown图源可编辑，未把图源存在称为UI渲染测试。
