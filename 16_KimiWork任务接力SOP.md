# KimiWork 任务接力 SOP

当一项工作需要在 Codex/WorkBuddy 与 KimiWork 之间接力推进时，用这份流程保证上下文不丢失、材料边界清楚、结果可核查。决策权始终在龚博士。

## 适用范围

- 适用于资料整理、文献高层综述、写作与排版、格式转换、非敏感数据分析、代码与工具链调试等。
- 涉及未发表数据、个人隐私或机构保密材料的任务，默认留在本环境完成，不外发。

## 口令与流程

### 第一步：龚博士输入「切换kimi」

可附一句任务说明（不附则按当前对话上下文整理）。主 agent 立即做两件事：

1. 生成交接记忆文档：`交接_Handoff/YYYY-MM-DD_<任务名>_交接.md`，只放完成任务所必需的信息，最小化、去标识化。
2. 输出一段**交接提示词**（模板见下，已填入实际日期与任务名），供龚博士直接复制。

### 第二步：龚博士打开 KimiWork，粘贴交接提示词

- 由龚博士手动打开 KimiWork，在**同一目录**（`danta-research-workbench`）下新建对话，把交接提示词粘贴进去；Kimi 读取交接文件后继续执行。
- 若主 agent 环境具备 computer use 且龚博士当场授权，可由 agent 代为打开 KimiWork；新建对话与粘贴仍需她在场确认。

### 第三步：Kimi 完成 → 写回传文件 → 输出回传提示词

交接提示词中已要求 Kimi：

1. 把执行结果写回 `交接_Handoff/YYYY-MM-DD_<任务名>_回传.md`（结果摘要 / 实际新建或修改的文件路径 / 未完成项 / 存疑点）。
2. 输出一段**回传提示词**，供龚博士复制回 Codex/GPT。

### 第四步：龚博士输入「切换kimi完成」（或粘贴回传提示词）

主 agent 读取回传文件并核查，然后接着执行后续工作：

1. 区分原始证据、模型摘要、推断与未知；关键主张回到允许访问的原始来源验证。
2. 核对实际改动的文件是否存在、内容是否符合交接要求。
3. 不因为另一模型给出了答案，就把它当作已验证事实或安全豁免。
4. 核查通过后继续推进任务；有出入时先向龚博士说明差异再动手。

## 提示词模板

### 交接提示词（主 agent 生成，龚博士复制给 Kimi）

```text
请先阅读本目录下的交接文件 `交接_Handoff/YYYY-MM-DD_<任务名>_交接.md`，严格按其中的任务目标、待办清单、相关文件路径和边界与禁忌继续执行。

要求：
1. 只做交接文件中列出的待办事项，不扩大范围，不触碰"边界与禁忌"中列出的内容。
2. 需要读写的文件按交接文件中的绝对路径操作。
3. 完成后，把结果写入 `交接_Handoff/YYYY-MM-DD_<任务名>_回传.md`，包含：结果摘要 / 实际新建或修改的文件路径 / 未完成项 / 存疑点。
4. 最后输出一段「回传提示词」供我复制给 Codex/GPT 继续工作，内容包括：回传文件路径、你实际改动过的文件清单、需要对方重点核查的事项、建议的下一步。
```

### 回传提示词（Kimi 生成，龚博士复制回 Codex/GPT）

```text
KimiWork 侧已完成本轮接力。请读取 `交接_Handoff/YYYY-MM-DD_<任务名>_回传.md`，按以下要求继续：

1. 核查回传内容：区分原始证据、模型摘要、推断与未知，关键主张回到原始来源验证。
2. 核对实际改动的文件：<Kimi 填写的文件清单>，确认文件存在且内容符合交接要求。
3. 重点核查事项：<Kimi 标注的存疑点>。
4. 核查通过后继续执行：<Kimi 建议的下一步>；如有出入，先向我说明差异再动手。
```

## 交接记忆文档模板

主 agent 生成「交接」文件时按此结构填写：

```markdown
# 任务交接记忆文档

- 任务名称：
- 交接日期：
- 交接方向：Codex/WorkBuddy → KimiWork

## 任务目标
（一句话说明要做出什么成果）

## 背景与口径
（必要的上下文、写作/格式要求、品牌或学术口径）

## 已完成工作
（已做了什么、产出文件的绝对路径）

## 待办清单
（按顺序列出剩余步骤，每步可独立验证）

## 相关文件
（需要读写的文件绝对路径清单）

## 边界与禁忌
（不要碰的目录/文件、不要外发的材料、风格禁忌）

## 期望回传
完成后请把结果写入 `交接_Handoff/<同名>_回传.md`，包含：
结果摘要 / 实际改动的文件路径 / 未完成项 / 存疑点
```

## 边界

- 每次外发前，主 agent 说明将传递给第三方平台的具体材料，取得龚博士当场同意；优先只传最小化、去标识化、非敏感材料。
- `交接_Handoff/` 是运行时私有目录，已加入 `.gitignore`，不提交公开仓库；不再需要时可整体归档或删除。
- 本流程不改变各平台规则与机构审查要求；可信访问申请仍按文件 15 的官方渠道核验。
