# 科研汇报与可编辑 PPT / Research Presentations

这套流程把论文、研究方案、分析结果和本周进展变成逻辑清楚、可在 PowerPoint 中继续修改的汇报稿。重点是先想明白“这次要讲清什么”，再制作幻灯片。组会的讨论提纲仍可只用 Markdown；只有需要演示文件时才生成 PPTX。

## 直接这样告诉 Codex

> 请根据我这次明确提供的材料，帮我做一份可编辑的组会 PPT。先整理核心问题、听众需要判断什么和逐页提纲，再生成 PPTX。把已完成结果、正在分析、计划中的分析和预期结果明确分开；每个研究结论保留来源，图表不要合并成整页图片。缺少的信息先标待核实，不编造。完成后逐页检查，并把 PPTX、可重建的 outline.json 和来源清单放到我的知识库相应成果目录。先读取已授权资料；如果需要扩大扫描范围，请先征得我同意。

如果她知道汇报时间、指定听众、学院模板或页数，可以顺手告诉主 agent；未知的可以留空。

## 工作流

1. **先确认目的。** 这次是组会、文献讨论、开题、阶段检查还是预答辩？最希望导师/评审帮助判断什么？已有记录能回答的部分不再重复问。
2. **整理证据。** 从本次提供或已授权的材料提取主张、来源位置和限制。文献结果、本人实验/分析、推断、假说和计划分开标记。
3. **写提纲。** 先定叙事和每页任务，再决定页数。页数根据可讲时间、证据量和学院要求变化，不固定 15 页。
4. **生成 PPTX。** 使用内置的 `presentation-skill`，保留源 `outline.json`。文本、表格、流程图尽量使用原生对象；科学图作为单独可替换图片并保留源文件或运行记录。
5. **检查与保存。** 核对来源、图注、状态、可读性和排版；需要返工时修改提纲并重建。输出放在私有知识库 `08_成果输出_Outputs/04_科研汇报与答辩_Presentations/`，回链到相应组会记录。

一段实质讨论结束时，主 agent 会先简短复盘；若结论适合面向导师或组会汇报，会问她是否要做成 PPT。回复需要即可进入上面的制作流程；不会因讨论结束自动开始做稿。

视频示例中的 NSCLC 研究材料只说明工作流如何运行；视频中的疾病背景、研究结论、分析结果及提议均须独立核查，不能直接移植为龚博士的研究事实。示例使用了某个自有平台，本工作台不依赖该平台。

## 安装与首次配置

从本工作台安装主引导时，PPT 引导和上游 `presentation-skill` 会一并安装，无需额外搜索同名技能。上游项目采用 MIT 许可，按固定 Git 提交打包，来源与许可见 [第三方许可清单](THIRD_PARTY_NOTICES.md)。

技能文件离线安装后，首次实际制作仍需在私有工作副本内配置生成环境。请 Codex 按安装目录的 `presentation-skill/SKILL.md` 操作：先检查 Node 与 Python，缺少 Node 依赖时执行 `npm install`，再执行 `npm run setup:python` 与 `npm run doctor`。`setup:python` 会在该技能目录建立本地虚拟环境并安装锁定的 Python 依赖。安装依赖会访问软件包源；遇到网络、权限或环境限制时先解释，不能声称配置已完成。不要把研究材料传到第三方演示平台；正常生成留在本机。LibreOffice/Poppler 是渲染复核的可选项，不是生成 PPTX 的前置条件。

默认用原生文字、形状和已授权的本地科研图，不联网搜图，也不调用上游提供的可选图像生成 API。若确实需要把提示词或研究内容发送到外部服务，先说清服务、发送内容和用途，取得龚博士明确授权。

## 保存内容

每次有实质汇报任务时，建议保存 PPTX、`outline.json`、简短来源清单和质检结果；源材料仍留在原位置，只在来源清单保留路径或 ID。会后把实际导师意见、本人理解、AI建议和待办分别归档，不能把自动生成内容记成导师认可或个人决定。

工作流细节见 [danta-research-ppt](skills/danta-research-ppt/SKILL.md) 和其 [证据与制稿流程](skills/danta-research-ppt/references/workflow.md)。
