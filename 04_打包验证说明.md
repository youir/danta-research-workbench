# v1.4 优化与验证说明

依据使用者于 2026-09-24 更新的工作台与课题助手内容优化，保留其 Idea 画布、关键追问、主张—证据映射、研究方法参照及持续研究能力。

## 修正

1. 重复逐项冻结会阻断思考，改为三个回看节点；本人选择与科学证明分开。
2. 候选解释不写成根本成因；已有证据与验证计划分开，不把“能设计实验”当作已有结果。
3. 阻塞只描述具体动作，不阻断可逆的草拟、查阅或比较。
4. 证据台账与 CSV 的职责明确；安装后的技能不再用失效相对链接指向研究工作区。
5. 重新生成与当前技能一致的校验清单，避免更新后无法安装。
6. 增加一个主引导、三个专家的真实协作配置与降级路径。
7. 公开版去除私人关系预填；提供本地工作副本脚本，避免研究内容继续写入仓库模板。

## 检查范围

两套主技能通过结构校验；安装、重复执行、同名冲突保留和完整性拒绝路径已测试。本地文档链接检查、TOML 语法、压缩包及解压验证由发布检查执行。当前本地 Codex CLI 0.155.0-alpha.16 的配置读取命令可完成；这不证明所有角色在使用者客户端已加载。

多 agent 配置依据官方文档核对；本轮已实际运行三个通用专家子 agent 的虚构交接与范围变更测试，见 tests/reports/v1.3-review.md。尚未在蛋挞设备上做真实科研端到端测试。没有伪造专家交接或把旧模拟脚本中的病种、样本、时间作为真实画像。旧测试报告仅作历史设计材料，未打包为验证证据。

检查入口：`python3 scripts/validate_release.py`。使用者首次运行仍需验证技能发现、实际子 agent 调用、文献访问与文件解析。未经真实调用不标记“多 agent 验证完成”。

本版进一步修复副本重复创建、复制范围和启动顺序，增加来源转述识别、任务版本及过时结果处理。专家讨论与采纳理由详见测试报告。

## v1.4 增量验证

新增模块的受限材料行为对比见 [测试报告](tests/reports/v1.4-skills-review.md)。发布验证覆盖11个安装目录、单选reader时自动补齐shared、共享依赖冲突时不写入reader、重复安装、完整性拒绝和工作副本边界。技能文件与许可证离线分发；没有自动安装MCP或全文处理依赖。
